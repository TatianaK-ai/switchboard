# Agent core
langchain-openai>=1.0
langchain-core>=1.0
langgraph>=1.0
langgraph-checkpoint-sqlite>=2.0

# Server (ElevenLabs calls these webhooks)
fastapi>=0.115
uvicorn[standard]>=0.32

# Config & validation
python-dotenv>=1.0
pydantic>=2.9

# Retrieval over the runbooks (no external vector store: the corpus is 12 documents)
rapidfuzz>=3.9

# Tests
pytest>=8.0
httpx>=0.27
